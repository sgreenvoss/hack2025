#define YSTART -10
#define YEND 10
#define XSTART -10
#define XEND 10
#define GRID_RES 0.1
#include <math.h>






v * GenerateGrid()
{
    v * Grid = (v *)malloc(sizeof(v) )
}
//void ** GeneratePoints(xStart, xEnd, xRes, yStart, yEnd, yRes, void *

for (int i=0; i<GRID_PTS/10; i++)
{
    for (int j=0; j<GRID_PTS/10; j++)
    {
        
    }
}

void generateEllipticParaboloid(float **vertices, unsigned int **indices, 
                                int segments_x, int segments_y, 
                                float max_x, float max_y, 
                                int *vertexCount, int *indexCount);
int main(){

}